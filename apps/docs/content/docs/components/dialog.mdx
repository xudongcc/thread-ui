---
title: Dialog
description: A responsive dialog component that displays as a Dialog on desktop and automatically switches to a Drawer on mobile devices.
---

<Preview path="dialog" />

## Installation

```bash
pnpm dlx shadcn@latest add @thread-ui/dialog
```

## Usage

```tsx
import {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/thread-ui/dialog";
```

```tsx
<Dialog>
  <DialogTrigger>Open</DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Dialog Title</DialogTitle>
      <DialogDescription>Dialog description goes here.</DialogDescription>
    </DialogHeader>
    <div>Your content here</div>
  </DialogContent>
</Dialog>
```

## Features

- **Responsive Design**: Automatically adapts to screen size
  - Desktop: Renders as a centered modal dialog
  - Mobile: Renders as a bottom drawer for better touch interaction
- **Seamless API**: Uses the same component API regardless of device type
- **Built on Radix UI**: Leverages Dialog and Drawer primitives for accessibility

## Best Practices

- Use clear, descriptive titles that explain the purpose of the dialog
- Keep dialog content focused and concise
- Provide a clear way to dismiss the dialog (close button or click outside)
- Use `DialogDescription` to provide additional context for screen readers

## API Reference

### DialogProps

The root component that wraps all dialog parts. Automatically detects device type and renders the appropriate component.

<AutoTypeTable path="../../packages/dialog/index.tsx" name="DialogProps" />

### DialogTriggerProps

The button that opens the dialog. Use `asChild` prop to use your own button component.

<AutoTypeTable
  path="../../packages/dialog/index.tsx"
  name="DialogTriggerProps"
/>

### DialogContentProps

The container for the dialog content that appears when the dialog is open.

<AutoTypeTable
  path="../../packages/dialog/index.tsx"
  name="DialogContentProps"
/>

### DialogHeaderProps

A container for the dialog title and description.

<AutoTypeTable
  path="../../packages/dialog/index.tsx"
  name="DialogHeaderProps"
/>

### DialogTitleProps

The title of the dialog. Should be used inside `DialogHeader`.

<AutoTypeTable path="../../packages/dialog/index.tsx" name="DialogTitleProps" />

### DialogDescriptionProps

The description of the dialog. Should be used inside `DialogHeader` after `DialogTitle`.

<AutoTypeTable
  path="../../packages/dialog/index.tsx"
  name="DialogDescriptionProps"
/>

### DialogFooterProps

A container for the dialog action buttons. Typically placed at the bottom of `DialogContent`.

<AutoTypeTable
  path="../../packages/dialog/index.tsx"
  name="DialogFooterProps"
/>

### DialogCloseProps

A button that closes the dialog. Use `asChild` prop to use your own button component.

<AutoTypeTable path="../../packages/dialog/index.tsx" name="DialogCloseProps" />
