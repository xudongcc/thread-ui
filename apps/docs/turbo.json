{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*",
        "$TURBO_ROOT$/components/**",
        "$TURBO_ROOT$/hooks/**",
        "$TURBO_ROOT$/libs/**",
        "$TURBO_ROOT$/themes/**",
        "$TURBO_ROOT$/locales/**"
      ]
    }
  }
}
