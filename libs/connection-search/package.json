{
  "name": "@repo/connection-search",
  "description": "Typed cursor pagination and DataFilter search schemas for Zod.",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  }
}
